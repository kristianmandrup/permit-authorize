# Permit

The permit is a container of permission rules. These rules are stored in a rule repository.
The permit is also the place where rules are declared via a DSL and then compiled (statically or dynamically) into
rule objects which are stored in the rule repository of the permit.

