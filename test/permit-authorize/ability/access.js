// Generated by LiveScript 1.2.0
(function(){
  var requires, obj, createUser, createRequest;
  requires = require('../../../requires');
  obj = requires.util('obj_util');
  createUser = requires.fac('create-permit');
  createRequest = requires.fac('create-request');
  module.exports = {
    empty: {},
    user: function(user){
      return createRequest.userAccess(user);
    },
    role: function(role){
      return createRequest.roleAccess('admin');
    },
    guest: function(){
      return this.role('guest');
    },
    admin: function(){
      return this.role('admin');
    },
    kris: {
      user: {
        name: 'kris'
      },
      action: 'read',
      ctx: {
        auth: true
      }
    },
    krisAdmin: import$(import$({}, this.admin), this.kris)
  };
  function import$(obj, src){
    var own = {}.hasOwnProperty;
    for (var key in src) if (own.call(src, key)) obj[key] = src[key];
    return obj;
  }
}).call(this);
