// Generated by LiveScript 1.2.0
(function(){
  var requires, Matcher, PermitRegistry, setup, Book, User, createUser, createMatcher, intersect, includes, users, permits, requests, ctx, context, matching, noneMatching, books;
  requires = require('../../../../../requires');
  requires.test('test_setup');
  Matcher = requires.permit('matcher').ContextMatcher;
  PermitRegistry = requires.permit('registry').PermitRegistry;
  setup = requires.fix('permits').setup;
  Book = requires.fix('book');
  User = requires.fix('user');
  createUser = requires.fac('create-user');
  createMatcher = function(ctx, ar, debug){
    debug == null && (debug = false);
    return new Matcher(ctx, 'includes', ar, debug);
  };
  intersect = function(obj){
    obj == null && (obj = {});
    return {
      intersect: obj
    };
  };
  includes = function(obj){
    obj == null && (obj = {});
    return intersect({
      includes: obj
    });
  };
  users = {};
  permits = {};
  requests = {};
  ctx = {};
  context = {};
  matching = {};
  noneMatching = {};
  users.kris = createUser.kris();
  requests.user = {
    user: users.kris
  };
  users.emily = createUser.emily;
  ctx.base = {
    ctx: {
      area: 'guest',
      secret: '123'
    }
  };
  ctx.andMore = {
    ctx: {
      area: 'guest',
      secret: '123',
      more: 'So much more...'
    }
  };
  ctx.andLess = {
    ctx: {
      area: 'guest'
    }
  };
  permits.user = setup.userPermit();
  books = {};
  describe('ContextMatcher (include)', function(){
    var matcher;
    before(function(){});
    return describe('include - intersect', function(){
      describe('includes user.name: kris', function(){
        beforeEach(function(){
          return matcher = createMatcher(includes(ctx.base), ctx.base);
        });
        return specify('matches access-request on includes intersect', function(){
          return matcher.match().should.be['true'];
        });
      });
      describe('includes empty {}', function(){
        beforeEach(function(){
          permits.user.includes = {};
          return matcher = createMatcher(includes(), requests.user);
        });
        return specify('matches access-request since empty includes always intersect', function(){
          return matcher.match().should.be['true'];
        });
      });
      return describe('includes is nil', function(){
        beforeEach(function(){
          permits.user.includes = void 8;
          return matcher = createMatcher(permits.user, requests.user);
        });
        return specify('does NOT match access-request since NO includes intersect', function(){
          return matcher.match().should.be['false'];
        });
      });
    });
  });
}).call(this);
