// Generated by LiveScript 1.2.0
(function(){
  var requires, mixin, expect;
  requires = require('../../../../requires');
  requires.test('test_setup');
  mixin = requires.rule('container').RuleMixin;
  expect = require('chai').expect;
  describe('RuleMixin', function(){
    var container, act, action;
    return context('basic repo', function(){
      before(function(){
        container = {};
        act = 'can';
        return action = 'edit';
      });
      return describe('valid-container', function(){
        return context('empty container', function(){
          return specify('throws', function(){
            return expect(mixin.validContainer(container, 'can')).to['throw'];
          });
        });
      });
    });
  });
}).call(this);
