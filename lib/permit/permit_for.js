// Generated by LiveScript 1.2.0
(function(){
  var requires, lo, Permit, toString$ = {}.toString;
  requires = require('../../requires');
  lo = require('lodash');
  Permit = requires.lib('permit');
  module.exports = function(baseClazz, name, baseObj, debug){
    var permit;
    if (toString$.call(baseClazz).slice(8, -1) === 'String') {
      baseObj = name;
      name = baseClazz;
      baseClazz = Permit;
    }
    permit = new baseClazz(name);
    if (debug === true) {
      permit.debugOn();
    }
    if (baseObj != null && _.isType('Function', baseObj)) {
      baseObj = baseObj();
    }
    if (toString$.call(baseObj).slice(8, -1) === 'Object') {
      permit = permit.use(baseObj);
    }
    return permit.init();
  };
}).call(this);
