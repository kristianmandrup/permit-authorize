// Generated by LiveScript 1.2.0
(function(){
  var objClass, toString$ = {}.toString;
  objClass = function(subject){
    if (subject.constructor.displayName) {
      return subject.constructor.displayName;
    }
    return subject.clazz || subject._clazz || subject.$clazz || subject._class || subject.$class;
  };
  module.exports = {
    camelCase: function(s){
      return (s || '').toLowerCase().replace(/(\b|-)\w/g, function(m){
        return m.toUpperCase().replace(/-/, '');
      });
    },
    clazzFor: function(subject){
      var res;
      res = (function(){
        switch (toString$.call(subject).slice(8, -1)) {
        case 'Object':
          return objClass(subject);
        case 'String':
          return subject;
        }
      }());
      return camelize(res || '');
    }
  };
}).call(this);
